name: iptv
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  audioplayers: ^6.0.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3 # Para encontrar o caminho para salvar o banco de dados
  video_player: ^2.8.6  # Para tocar o vídeo
  wakelock_plus: ^1.2.2   # Para manter a tela acesa
  intl: ^0.19.0 # Adicione esta linha



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.9


flutter:
  
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/audio/ # Adicione esta linha
  